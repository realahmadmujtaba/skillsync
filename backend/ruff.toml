line-length = 100
target-version = "py312"

[lint]
select = ["E", "F", "I", "W"]
# E501 (line length) is handled by the formatter; migrations may exceed it.
ignore = ["E501"]

[lint.per-file-ignores]
"migrations/*" = ["F401"]
"tests/*" = ["F401"]
